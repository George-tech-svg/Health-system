services:
  - type: web
    name: fastafya
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:\n    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
